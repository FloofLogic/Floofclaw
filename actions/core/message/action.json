{
  "id": "message",
  "description": "Send a user-facing message back to the originating channel.",
  "outside_world": true,
  "args_schema": {
    "type": "object",
    "required": ["message"],
    "additionalProperties": false,
    "properties": {
      "message": {
        "type": "string",
        "description": "Text to deliver to the user: at most 8192 bytes of UTF-8. A longer message is refused with the limit named, never truncated; split a long answer across turns."
      }
    }
  },
  "exec": ["runtime", "fc_message()"],
  "timeout_ms": 0
}
