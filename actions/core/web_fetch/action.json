{
  "id": "web_fetch",
  "description": "Fetch an HTTP or HTTPS URL and return response metadata and text content. Managed operation: call with op:start; it finishes synchronously and a later operation_result turn receives text containing the HTTP status and bounded body excerpt. The full body stays in the action-call artifact named by body_artifact in action_succeeded.",
  "outside_world": true,
  "args_schema": {
    "type": "object",
    "required": [
      "op"
    ],
    "additionalProperties": false,
    "properties": {
      "op": {
        "type": "string",
        "enum": [
          "start"
        ],
        "description": "'start' fetches and finishes immediately."
      },
      "url": {
        "type": "string",
        "description": "HTTP or HTTPS URL to fetch. Required with op:start."
      }
    }
  },
  "exec": [
    "bash",
    "run.sh"
  ],
  "timeout_ms": 45000,
  "contract": "managed_operation",
  "default_timeout_ms": 90000,
  "max_timeout_ms": 180000,
  "config": [
    {
      "name": "SANDBOX",
      "key": "sandbox",
      "kind": "string",
      "required": false
    }
  ]
}
